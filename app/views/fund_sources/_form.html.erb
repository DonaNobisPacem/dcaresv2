<%= simple_form_for(@fund_source) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.input :source_name %>
  	<%= f.input :fund_source_classification %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, :class => "btn btn-success" %>
  </div>
<% end %>
