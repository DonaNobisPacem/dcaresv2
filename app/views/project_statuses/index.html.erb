<% if current_user.admin %>
<p id="notice"><%= notice %></p>
</br>
<h1><b>List of Project Statuses</b></h1>
<%= link_to '<i class="fa fa-plus"></i> New Project Status'.html_safe, new_project_status_path, :class => "pull-right" %>
<table class="table table-hover table-responsive">
  <thead>
    <tr>
      <th>Description</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @project_statuses.each do |project_status| %>
      <tr>
        <td><%= project_status.description %></td>
        <td style="text-align: right;">
           <%= link_to 'Edit', edit_project_status_path(project_status) %> | <%= link_to 'Destroy', project_status, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
 <%= render 'shared/unauthorized' %>
<% end %>

