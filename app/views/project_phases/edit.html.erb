<h1>Editing <%= link_to @project_phase.phase_name, project_project_phase_path(@project_phase.project, @project_phase) %></h1>

<% if current_user.admin || !current_user.universities.where(:id => @project_phase.project.university_id ).blank? %>
	<%= render 'form' %>
<% else %>
	<%= render 'shared/unauthorized' %>
<% end %>

<%= link_to 'Show', project_project_phase_path(@project_phase.project, @project_phase) %> |
<%= link_to 'Back', "javascript:history.back()" %>